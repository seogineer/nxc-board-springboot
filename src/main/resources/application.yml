spring:
  # Database Settings
  datasource:
    url: jdbc:h2:mem:testdb;MODE=mysql;
    platform: h2
    username: sa
    password:
    driverClassName: org.h2.Driver

  jpa:
    show-sql: true
  h2:
    console:
      enabled: true